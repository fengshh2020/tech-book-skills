# 边界用例

> **[建议]** 这是一个多段落 callout，用来验证侧边栏能容纳多段内容。
>
> 第二段，含 **加粗** 和 `行内代码`，确认内联标记在 callout 内仍生效。
>
> - 列表项一
> - 列表项二

## callout 内嵌代码块

> **[检查清单]** 验证步骤如下：
>
> ```python
> assert 1 + 1 == 2
> ```

## 图片 + 图注（figure 约定）

![数据流](diagrams/mmd-71fa310c06.png "图：复用的最小数据流")

## 术语首次出现

Zenoh 的核心是 **会话**（Session），管理所有网络连接。

## 嵌套列表

1. 顶层项
   - 嵌套 a
   - 嵌套 b
2. 另一个顶层项

## 表格内含行内代码

| 方法 | 签名 |
|------|------|
| put | `put(key, value)` |
| get | `get(key)` |
